import { GenerateSchemaIdPipe } from './generate-schema-id.pipe';

describe('GenerateSchemaIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateSchemaIdPipe();
    expect(pipe).toBeTruthy();
  });
});
